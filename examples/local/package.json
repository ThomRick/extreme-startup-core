{
  "name": "extreme-startup-core-local-example",
  "version": "0.0.0",
  "description": "An extreme startup local game example",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "build:clean": "npm run -s clean && npm run -s build",
    "start": "node index.js",
    "clean": "rm -rf lib/*"
  },
  "author": "ThomRick",
  "license": "MIT",
  "dependencies": {
    "extreme-startup-core": "git+https://github.com/ThomRick/extreme-startup-core.git#CORE-0004"
  },
  "devDependencies": {
    "typescript": "^2.3.2"
  }
}
